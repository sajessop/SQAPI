% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_laser_scale.R
\name{calc_manual_scale}
\alias{calc_manual_scale}
\title{Calculate scale from a manually supplied cm-per-pixel value}
\usage{
calc_manual_scale(
  media,
  media_col = "point.media.key",
  width_col = "pixel_width",
  height_col = "pixel_height",
  scale_col = "cm_per_pixel"
)
}
\arguments{
\item{media}{A data frame with one row per image, containing
\code{media_col}, \code{width_col}, \code{height_col}, and \code{scale_col}.}

\item{media_col}{Column identifying the image (default
\code{"point.media.key"}, matching Squidle+ annotation exports).}

\item{width_col, height_col}{Columns holding image pixel width/height.}

\item{scale_col}{(manual) Column already containing scale in cm per
pixel.}
}
\value{
See \code{\link[=calc_image_scale]{calc_image_scale()}}. \code{pixel_distance} is \code{NA} and
\code{scale_method} is \code{"manual"}.
}
\description{
For images where scale is already known — e.g. a fixed-geometry rig
with a pre-measured footprint, or a value pulled from a prior
calibration — rather than measured from laser points or estimated from
altitude. Called by \link{calc_image_scale(method = "manual")}.
}
