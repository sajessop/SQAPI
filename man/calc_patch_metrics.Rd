% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_fragstats.R
\name{calc_patch_metrics}
\alias{calc_patch_metrics}
\title{Compute one or more FRAGSTATS-style patch-level metrics}
\usage{
calc_patch_metrics(
  patches,
  metrics = "all",
  keep_geometry = TRUE,
  scale = NULL
)
}
\arguments{
\item{patches}{An sf data frame from \code{\link[=build_patch_sf]{build_patch_sf()}}. If it carries
\code{valid_geom}/\code{touches_edge} flag columns from \code{build_patch_sf()},
they're preserved in the output alongside the requested metrics —
filter on them explicitly if you didn't already remove flagged
patches upstream.}

\item{metrics}{Character vector of metric names to compute, or \code{"all"}
(default) for every metric in \link{patch_metric_registry}.}

\item{keep_geometry}{If \code{TRUE} (default) returns an sf object; if
\code{FALSE} returns a plain data frame.}

\item{scale}{Real-world scale to convert pixel-based area/perimeter
before computing metrics: \code{NULL}, a single numeric, or a per-media
data frame from \code{\link[=calc_image_scale]{calc_image_scale()}}.}
}
\value{
A data frame (sf or plain) with id columns \code{media_id}, \code{class},
\code{patch_id} (plus any flag columns present on \code{patches}), plus one
column per requested metric.
}
\description{
Compute one or more FRAGSTATS-style patch-level metrics
}
