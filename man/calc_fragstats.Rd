% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_fragstats.R
\name{calc_fragstats}
\alias{calc_fragstats}
\title{Compute FRAGSTATS-style metrics from an annotation export in one call}
\usage{
calc_fragstats(
  ann,
  x_col = "point.x",
  y_col = "point.y",
  polygon_col = "point.polygon",
  label_col = "label.name",
  media_col = "point.media.id",
  media_width = 1,
  media_height = 1,
  flag_invalid = TRUE,
  remove_invalid = FALSE,
  fix_invalid = FALSE,
  flag_edge = TRUE,
  remove_edge = FALSE,
  edge_vertex_threshold = 2,
  edge_tolerance = 1e-06,
  level = "all",
  patch_metrics_wanted = "all",
  media_area = NULL,
  scale = NULL
)
}
\arguments{
\item{ann}{A data frame with an anchor point, polygon vertex list, class
label, and media identifier column, spanning as many images/classes as
you like — this is not scoped to one image or one class.}

\item{x_col, y_col}{Column names for the anchor point.}

\item{polygon_col}{Column name holding the vertex list/JSON.}

\item{label_col}{Column name holding the class label.}

\item{media_col}{Column name identifying the media item (image).}

\item{media_width, media_height}{Passed to \code{\link[=polygon_to_sf]{polygon_to_sf()}}. Use the
actual image width/height in pixels if you plan to apply a per-pixel
\code{scale} downstream (see \code{\link[=calc_image_scale]{calc_image_scale()}}), and note these are also
used as the image border for edge-touch detection.}

\item{flag_invalid}{If \code{TRUE} (default), add a \code{valid_geom} column
(\code{sf::st_is_valid()} result) to the output.}

\item{remove_invalid}{If \code{TRUE}, drop patches with invalid geometry from
the output entirely. Checked \emph{after} \code{fix_invalid}, so only patches
that remain invalid after an attempted repair are removed.}

\item{fix_invalid}{If \code{TRUE}, attempt to repair invalid geometries with
\code{sf::st_make_valid()} before the validity check/removal above.}

\item{flag_edge}{If \code{TRUE} (default), add \code{n_border_vertices} and
\code{touches_edge} columns marking patches whose polygon touches the image
frame.}

\item{remove_edge}{If \code{TRUE}, drop patches flagged as touching the edge
from the output entirely.}

\item{edge_vertex_threshold}{Minimum number of vertices lying on the
image border for a patch to be flagged/removed as an edge patch
(default \code{2}; a single border vertex can happen by coincidence, two or
more strongly suggests the boundary genuinely runs along the frame).}

\item{edge_tolerance}{Numeric tolerance (same units as
\code{media_width}/\code{media_height}) for considering a vertex "on" the
border, to allow for floating-point imprecision.}

\item{level}{Which output(s) to compute: any combination of \code{"patch"},
\code{"class"}, \code{"landscape"}, or \code{"all"} (default).}

\item{patch_metrics_wanted}{Character vector of patch metric names (see
\link{patch_metric_registry}) to include in the \code{patch} output, or \code{"all"}.}

\item{media_area}{Passed to \code{\link[=class_metrics]{class_metrics()}} / \code{\link[=landscape_metrics]{landscape_metrics()}}.}

\item{scale}{Real-world scale to apply before computing any metric:
\code{NULL}, a single numeric, or a per-media data frame from
\code{\link[=calc_image_scale]{calc_image_scale()}} — typically built from \code{pose.pose.alt} plus
camera intrinsics, or laser point separation where available.}
}
\value{
A list containing whichever of \code{patch}, \code{class}, \code{landscape}
were requested via \code{level}.
}
\description{
Runs the full pipeline — geometry reconstruction, validity/edge
flagging, scaling, and metric computation — across every image and
class present in \code{ann}.
}
\examples{
\dontrun{
ann <- get_annotations_by_set(api, annotation_set_id = 5432)
media <- get_media_by_campaign(api, campaign_id = 123)

img_scale <- calc_image_scale(media, method = "altitude",
                               altitude_col = "pose.pose.alt",
                               hfov_deg = 54, image_width_px = 4000)

# flag (but keep) invalid/edge patches, exclude both from area stats
fs <- calc_fragstats(ann, media_width = 4000, media_height = 3000,
                      scale = img_scale,
                      remove_invalid = TRUE, remove_edge = TRUE)
fs$landscape
}
}
