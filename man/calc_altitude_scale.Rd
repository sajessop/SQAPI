% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_laser_scale.R
\name{calc_altitude_scale}
\alias{calc_altitude_scale}
\title{Calculate scale from camera altitude and intrinsics (pinhole model)}
\usage{
calc_altitude_scale(
  media,
  media_col = "point.media.key",
  width_col = "pixel_width",
  height_col = "pixel_height",
  altitude_col = "pose.pose.alt",
  sensor_width_mm = NULL,
  focal_length_mm = NULL,
  hfov_deg = NULL
)
}
\arguments{
\item{media}{A data frame with one row per image, containing
\code{media_col}, \code{width_col}, \code{height_col}, and \code{altitude_col}.}

\item{media_col}{Column identifying the image (default
\code{"point.media.key"}, matching Squidle+ annotation exports).}

\item{width_col, height_col}{Columns holding image pixel width/height.}

\item{altitude_col}{(altitude) Column with camera altitude above the
seafloor, e.g. \code{"pose.pose.alt"}, in metres.}

\item{sensor_width_mm, focal_length_mm}{(altitude, pinhole model) Camera
sensor width and focal length in mm. Provide these OR \code{hfov_deg}.}

\item{hfov_deg}{(altitude, FOV model) Horizontal field of view of the
camera in degrees. Provide this OR \code{sensor_width_mm} +
\code{focal_length_mm}.}
}
\value{
See \code{\link[=calc_image_scale]{calc_image_scale()}}. \code{pixel_distance} is \code{NA} and
\code{scale_method} is \code{"altitude"}.
}
\description{
Fallback for images without laser points and without a known manual
scale: derives cm-per-pixel from camera altitude above the seafloor
(\code{pose.pose.alt}) plus either sensor width + focal length, or horizontal
field of view. Assumes near-nadir imagery over a locally flat seafloor
— least accurate of the three methods, prefer laser or manual scale
where available. Called by \link{calc_image_scale(method = "altitude")}.
}
