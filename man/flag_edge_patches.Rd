% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_fragstats.R
\name{flag_edge_patches}
\alias{flag_edge_patches}
\title{Flag patches whose polygon touches the image border}
\usage{
flag_edge_patches(
  patches,
  media_width,
  media_height,
  edge_vertex_threshold = 2,
  edge_tolerance = 1e-06
)
}
\arguments{
\item{patches}{An sf data frame of patches, in the same coordinate units
as \code{media_width}/\code{media_height}.}

\item{media_width, media_height}{Image dimensions defining the border.}

\item{edge_vertex_threshold}{Minimum number of border-touching vertices
to flag a patch as an edge patch (default \code{2}).}

\item{edge_tolerance}{Numeric tolerance for considering a vertex "on"
the border.}
}
\value{
\code{patches} with \code{n_border_vertices} and \code{touches_edge} columns
added/updated.
}
\description{
Standalone version of the edge-touch check performed inside
\code{\link[=build_patch_sf]{build_patch_sf()}} — useful if you already have an sf patch data frame
and just want to (re-)flag it against a given frame size, recomputing
border-touch counts directly from geometry rather than the original
relative vertex list.
}
