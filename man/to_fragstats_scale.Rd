% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_laser_scale.R
\name{to_fragstats_scale}
\alias{to_fragstats_scale}
\title{Convert calc_image_scale() output into the format expected by the
FRAGSTATS patch-metric functions}
\usage{
to_fragstats_scale(image_scale, media_id_col = "media_id", unit = c("m", "cm"))
}
\arguments{
\item{image_scale}{Output of \code{\link[=calc_image_scale]{calc_image_scale()}} (or
\code{\link[=calc_laser_scale]{calc_laser_scale()}} / \code{\link[=calc_manual_scale]{calc_manual_scale()}} / \code{\link[=calc_altitude_scale]{calc_altitude_scale()}}
directly).}

\item{media_id_col}{Name to use for the id column in the output
(default \code{"media_id"}, matching \code{\link[=build_patch_sf]{build_patch_sf()}}'s default
\code{media_col = "point.media.id"} after renaming — adjust if your
annotation export's media identifier differs from
\code{point.media.key}).}

\item{unit}{Real-world unit to report scale in: \code{"m"} (default, m/px)
or \code{"cm"} (cm/px, matching \code{cm_per_pixel} directly).}
}
\value{
A data frame with columns \code{media_id} and \code{scale}, ready to pass
as the \code{scale} argument to the FRAGSTATS metric functions.
}
\description{
\code{\link[=calc_patch_metrics]{calc_patch_metrics()}} / \code{\link[=class_metrics]{class_metrics()}} / \code{\link[=landscape_metrics]{landscape_metrics()}} /
\code{\link[=calc_fragstats]{calc_fragstats()}} expect a \code{scale} data frame with columns \code{media_id}
(matching whatever \code{media_col} was used in \code{\link[=build_patch_sf]{build_patch_sf()}}) and
\code{scale} in real-world units \emph{per pixel} (e.g. m/px), whereas
\code{\link[=calc_image_scale]{calc_image_scale()}} reports \code{cm_per_pixel} keyed on \code{point.media.key}.
This bridges the two.
}
